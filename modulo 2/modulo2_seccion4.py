#VARIABLES
var = 1
print(var)
#el resultado es 1

# Consiste de dos simples instrucciones:

# La primera crea una variable llamada var, y le asigna un literal con un valor entero de 1.
# La segunda imprime el valor de la variable recientemente creada en la consola.

#COMO EMPLEAR UNA VARIABLE
var = 1
account_balance = 1000.0
client_name = 'John Doe'
print(var, account_balance, client_name)
print(var)

#el resultado es 1, 1000.0 y John Doe
#el resultado es 1

var = 1
print(Var)
#no se permite usar una variable que no existe
#Se ha tratado de utilizar la variable llamada Var, la cual no tiene ningún valor (nota: var y Var son entidades diferentes, y no tienen nada en común dentro de Python).


#como asignar un nuevo valor a la variable
var = 100
var = 200 + 300
print(var)

#el resultado es 500

# LAB DE VARIALBES

var_john = 3
var_mary = 5
var_adam = 6
print(var_john, var_mary, var_adam, sep=', ')

#el resultado es 3, 5 y 6
total_apples = var_john + var_mary + var_adam
print(total_apples)
#el resultado es 14

print("Total number of apples:", total_apples)

